FROM mcr.microsoft.com/devcontainers/base:jammy

# Ubuntu 22.04 (Jammy) does NOT have the broken Yarn GPG repo
# that was present in universal:2 (Ubuntu 20.04 Focal).
# This allows PHP feature (injected by Codespaces) to run apt-get update successfully.
